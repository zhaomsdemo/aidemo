spring:
  application:
    name: aidemo

  ai:
    ollama:
      init:
        pull-model-strategy: when_missing
        chat:
          additional-models:
            - gpt-4-turbo-preview
      chat:
        options:
          temperature: 0.7